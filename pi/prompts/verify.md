# Verify Agent Output

The agent has finished working. Below is the full output from the agent session.
Enter Verification Mode now.

Do not summarize what the agent says it did. Verify it independently using:
- `backlog task <N> --plain` to read the ACs
- `git diff HEAD~1` to see what actually changed
- `uv run pytest` to confirm tests pass

Then report your findings to the user with specific file/line references and
ask: **agree / disagree / needs changes**

## Agent Output

$@
